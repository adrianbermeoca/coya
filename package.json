{
  "name": "coya",
  "version": "1.0.0",
  "description": "Comparador de tasas de cambio en tiempo real - COYA",
  "main": "server.js",
  "scripts": {
    "postinstall": "npx puppeteer browsers install chrome",
    "start": "cross-env NODE_ENV=production node server.js",
    "dev": "cross-env NODE_ENV=development VERBOSE_LOGGING=true node server.js",
    "prod": "cross-env NODE_ENV=production node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "exchange-rate",
    "currency",
    "peru",
    "dollar",
    "scraper"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.5.0",
    "chromedriver": "^143.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.1.0",
    "node-cron": "^4.2.1",
    "puppeteer": "^24.34.0",
    "selenium-webdriver": "^4.39.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
